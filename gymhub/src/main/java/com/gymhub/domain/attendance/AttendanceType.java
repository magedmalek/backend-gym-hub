package com.gymhub.domain.attendance;

public enum AttendanceType {

    /** Regular member visit linked to an active subscription. */
    MEMBER_VISIT,

    /** Guest visit recorded as part of a member's invitation quota. */
    GUEST_VISIT
}
