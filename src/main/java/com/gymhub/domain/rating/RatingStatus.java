package com.gymhub.domain.rating;

/**
 * Lifecycle status of a {@link RatingReview} (Phase 2 plan — Section 25).
 */
public enum RatingStatus {
    PENDING,
    PUBLISHED,
    HIDDEN,
    REPORTED
}
