package com.gymhub.domain.training;

/**
 * Movement category of an exercise (Phase 2 plan — Section 11).
 */
public enum ExerciseType {
    STRENGTH, CARDIO, WARM_UP, STRETCHING, MOBILITY, FUNCTIONAL,
    BODYWEIGHT, MACHINE, FREE_WEIGHT, OTHER
}
